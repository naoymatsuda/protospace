.container
　%form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    %h2.form-signin-heading Please sign in
    %label.sr-only{for: "inputEmail"} Email address
    %input#inputEmail.form-control{f.email_field :email, placeholder: "Email address", required: "", type: "email"}/
    %label.sr-only{for: "inputPassword"} Password
    %input#inputPassword.form-control{placeholder: "Password", required: "", type: "password"}/
    .checkbox
      %label
        %input{type: "checkbox", value: "remember-me"}/
        Remember me
    %button.btn.btn-lg.btn-primary.btn-block{type: "submit"} Sign in
  .text-center
     % f.text_field :mail
     % f.text_field :password
    %p
      Not a member?
     % if user_signed_in?
    %a{href: "/users/sign_in"} Sign up now
  %end

