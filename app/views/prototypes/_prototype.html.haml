.col-sm-4.col-md-3.proto-content.eachbox
  - if user_signed_in?
    - if prototype.user_id == current_user.id
      .dropdown.drop_menu_location
        = button_tag "ACTION", class: "btn btn-default dropdown-toggle", data: {toggle: "dropdown"}, id: "dropdownMenu", aria: {expanded: "false"}, type: "button", name: nil
          %ul.dropdown-menu{aria: {labelledby: "dropdownMenu1"}}
            %li
              = link_to 'Delete', prototype_path(prototype) , method: :delete
            %li
              = link_to 'Edit', edit_prototype_path(prototype)
  .thumbnail
    = link_to image_tag(prototype.main_image), prototype_path(prototype)
    .caption
      %h3
        = prototype.title
      .proto-meta
        .proto-user
          %p= link_to prototype.user.name, user_path(prototype.user)
        .proto-posted
          = prototype.created_at.strftime('%Y年%m月%d日')
      %ul.proto-tag-list.list-inline
        %li
          %a{href: "#", class: "btn btn-default"}
            iPad
        %li
          %a{href: "#", class: "btn btn-default"}
            wireframe
